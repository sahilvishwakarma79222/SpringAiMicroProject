spring:
  application:
    name: service-discovery
  rabbitmq:
    .host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
